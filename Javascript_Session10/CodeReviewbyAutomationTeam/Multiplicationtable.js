// To generate multiplication table of the given number

let num = 9;
//Space format is missing and allignment is missing
for (let i = 1; i<=10; i++) {
    const output = i * num;

console.log (output);
}